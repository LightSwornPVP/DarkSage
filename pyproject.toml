[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.mypy]
strict = true
python_version = "3.14"
exclude = ["backend/alembic/"]

[[tool.mypy.overrides]]
module = "aiosqlite.*"
ignore_missing_imports = true
