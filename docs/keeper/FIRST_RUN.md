# First Run

The Back/Next setup sequence explains authority boundaries, confirms the evidence
directory, accepts an optional first repository, shows providers and diagnostics,
and leads into the mock demonstration. Invalid writable paths or repositories keep
the user on the current step. Optional providers remain visibly unavailable.

The default evidence location can be changed through the application service and is
stored without credentials. A writable local data directory is mandatory.
