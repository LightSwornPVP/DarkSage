# First Run

The setup window explains Keeper's authority boundaries and reports Git, Python,
data-directory, and provider status. Complete safe setup, add an existing repository
from Projects, then run the deterministic demonstration from Home. Optional
providers remain visibly unavailable when their executable is absent.

The default evidence location can be changed through the application service and is
stored without credentials. A writable local data directory is mandatory.
