# Codex Companion — Traceability

This directory defines traceability between requirements, decisions, implementation, tests, and releases.

Use `TRACEABILITY_MATRIX.csv` as the machine-readable index.

Required relationship:
`Requirement → Design/ADR → Source → Test → Release/Change`
